<div class="footer">
  <div class="social-links">
    <%= link_to(sanitize("#{fa_icon("instagram")}"), "#{t("social_links.instagram")}", class: "social-link") %>
    <%= link_to(sanitize("#{fa_icon("facebook")}"), "#{t("social_links.facebook")}", class: "social-link") %>
    <%= link_to(image_tag("hobbynight70.png", class: "hnic-icon"), "#{t("social_links.hnic")}", class: "social-link") %>
  </div>
</div>